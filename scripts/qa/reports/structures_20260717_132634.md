# Structure QA — QA_STRUCT_132458

- Activated (region YAML): **0 / 1**
- Built (build-reqs): **1 / 1**
- Missing YAML keys: none

For each type: allocate NON-OVERLAPPING pad (r_a+r_b+margin) on a fresh origin → parse build-reqs → WE/fill → Civs place at center → region YAML

## Pad layout (non-overlapping)

- Origin: `[2760, 81, 2600]` margin=`10` blocked_existing=`10`
- `gravel_quarry` center=`[2760, 81, 2600]` r=`3` zone=`outer`

## Inventory before

- Hand slot 0: civs=`gravel_quarry` id=`minecraft:chest`
- hotbar[0] civs=`gravel_quarry` `minecraft:chest`
- hotbar[8] civs=`None` `minecraft:wooden_axe`

## Structures

- **gravel_quarry** (raw) — FAIL — build=True place=False center=[2760, 81, 2600] — manual build-reqs | pad r=3 sep_origin=(2760,2600) | center place missed (no chest)
  - held: slot=0 civs=gravel_quarry id=minecraft:chest

## Wand preflight

- `{'ok': True, 'command': '//wand', 'dry_run': False, 'focused': True, 'menus_closed': True, 'error': None, 'close_path': 'pause_dismiss_only'}`

## Log errors (tail)

- `(none)`

## Item acquisition

Uses inventory civs:civs key if present; else item replace with PDC (QA-only, not /cv give). Shop/tutorial still preferred for realism.