# Hermes → MCP E2E evidence (2026-08-10)

Labels: **FACT** · **OBSERVED** · **PASS** · **BLOCKED**

Hardware: WSL Ollama `100.69.136.92:11434`, RTX 3060 12GB. Windows Hermes v0.19.0.
Model: `hermes-fast`. Gateway log: `gateway-tools.jsonl`.

## A — observe + rpg_observe (no modify)

| Check | Result | Label |
|-------|--------|-------|
| `hermes -z` tool calls | `minecraft_observe`, `minecraft_rpg_observe` | **PASS** |
| Reported state | Steve @ 801.3/-60/800.5, health 20/20, CREATIVE; archetype merchant; active `[merchant_path, rescue_stonemason, rescue_treasurer]` | **OBSERVED** |
| Gateway log | both tools `status=PASS` | **FACT** |
| Wall time | ~48s | **FACT** |

## B — observe → move_to → observe

| Check | Result | Label |
|-------|--------|-------|
| First observe | logged PASS | **FACT** |
| `minecraft_move_to` args | `{x:803.3,y:-60,z:800.5,timeout_ms:20000}` (= prior x+2) | **FACT** |
| move_to status | PASS (Hermes also reported final_distance≈1.2 / coordinates changed true) | **PASS** |
| Second observe | logged PASS after move | **FACT** |
| Wall time | ~48s | **FACT** |

Note: post-session direct harness observe returned `player_offline` (actor disconnect after oneshot) — expected; in-session evidence is the gateway log + Hermes report.

## C — quest accept (abandon slot → accept)

| Check | Result | Label |
|-------|--------|-------|
| Path | Windows Hermes `-z` oneshot `hermes-fast` (~45s) | **FACT** |
| Actions | abandon `rescue_treasurer` (free max-active) → accept `rescue_quartermaster` → `minecraft_rpg_observe` | **FACT** |
| Hermes final line | `QUEST_ACCEPT_E2E PASS quest_id=rescue_quartermaster active=["merchant_path","rescue_stonemason","rescue_quartermaster"]` | **PASS** |
| Deterministic harness re-observe | `active_quests` includes `rescue_quartermaster`; `rescue_treasurer` absent | **PASS** |
| Lesson | Prefer `minecraft_rpg_accept` / `QuestAcceptResult` (+ observe), never bare `performCommand` | **FACT** |

## Gateway observability

Append-only JSONL (no secrets): `integration-tests/runner/reports/gateway-tools.jsonl`
fields: `ts`, `player`, `tool`, `args`, `status`, `latency_ms`, `result_preview`.
