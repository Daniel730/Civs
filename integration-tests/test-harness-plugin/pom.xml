<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.civs.itest</groupId>
    <artifactId>civs-test-harness</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>
    <name>CivsTestHarness</name>
    <description>Server-side integration-test harness for Civs: exposes internal-state
        inspection and assertion commands, driven over RCON by the scenario runner.</description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>herocraft</id> <!-- Vault Repo -->
            <url>https://nexus.hc.to/content/repositories/pub_releases/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>26.1.2.build.72-stable</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>net.milkbowl.vault</groupId>
            <artifactId>VaultAPI</artifactId>
            <version>1.7</version>
            <scope>provided</scope>
        </dependency>
        <!-- Civs is resolved from the sibling build output (system scope), like civs-quests. -->
        <dependency>
            <groupId>org.redcastlemedia.multitallented</groupId>
            <artifactId>civs</artifactId>
            <version>1.11.7</version>
            <scope>system</scope>
            <systemPath>${project.basedir}/../../target/civs-1.11.7.jar</systemPath>
        </dependency>
    </dependencies>

    <build>
        <finalName>CivsTestHarness</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
