# Place smoke — 20260717_120840

## Verdict: **PASS**

- Center: `2170 81 2170`
- `execute if block … chest` → exact_cy=`True` (neighbors `0,0,0: HIT`)
- Aim: levitation hover + pitch **90** (floor-top)
- Hotbar: Civs slot 0 verified before RMB; wand slot 8 preserved
- F2: `reports/screenshots/place_smoke_cy_f2_*.png`

## Root causes fixed

1. RCON `execute if block` returns `Test passed` — old `run say HIT` checks always failed.
2. Side/south aim hit neighbor tops or south face → chest at `cz-1` / `cy-1,cz-1`.
3. Top-down world view false-positive as pause — bypass vision gate when pitch=90.

## Harness

`place_civs_at_center` now prefers `overhead_down` and checks block before vision post.
